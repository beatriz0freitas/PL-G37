# Interface de linha de comando.
#
# Quando implementar:
# - aceitar um ficheiro Fortran e opções (ex: --stage, --emit-ir, --emit-vm)
# - pipeline típico: lexer -> parser -> semântica -> IR -> otimização -> codegen


